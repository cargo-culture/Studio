# Studio-enabled repository

This project uses the Three-Agent Studio operating model. See `AGENTS.md` and `.studio/studio.yaml` for governing rules. Human approval is required before substantive merge to `main`.
